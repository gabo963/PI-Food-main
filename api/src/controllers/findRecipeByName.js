const { Recipe } = require("../db");

const findRecipesByName = async ( name ) => {
    // Consultar en la base de datos interna y hacer un querie a spoonacular. Retornar los mismos campos en ambas consultas.

};